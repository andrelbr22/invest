[project]
name = "investment-engine"
version = "0.10.0"
description = "Formação do Investidor V1.9.0 with private backtest history and an official weekly strategy catalog."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
