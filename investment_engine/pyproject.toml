[project]
name = "investment-engine"
version = "0.11.3"
description = "Formação do Investidor V1.10.3 with auditable official backtest jobs."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
