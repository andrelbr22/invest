# Atualização V1.12.2

Esta atualização consolida a aplicação no servidor Oracle e elimina instruções
da hospedagem anterior.

Como foi detectada uma cópia duplicada do projeto dentro da pasta local
`investment_engine`, use o pacote completo em uma pasta nova. Isso evita levar
arquivos antigos e duplicados para o GitHub.

Os segredos do servidor não fazem parte do pacote e não são alterados.
