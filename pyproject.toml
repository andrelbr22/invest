[project]
name = "investment-engine"
version = "0.8.1"
description = "Formação do Investidor V1.7.1 with callback-safe quantity controls, isolated portfolios and personal saved screeners."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
