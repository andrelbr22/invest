[project]
name = "investment-engine"
version = "0.8.4"
description = "Formação do Investidor V1.7.4 with compatible portfolio purchase saving and fast quantity controls."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
