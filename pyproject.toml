[project]
name = "investment-engine"
version = "0.13.2"
description = "Formação do Investidor V1.12.2 self-hosted on Oracle with canonical domain references."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
