[project]
name = "investment-engine"
version = "0.15.2"
description = "Formação do Investidor V1.14.2 with restored analysis filters, Graham ordering and complete B3 alert catalog."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
