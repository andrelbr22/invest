[project]
name = "investment-engine"
version = "0.16.0"
description = "Formação do Investidor V1.15.0 with SaaS workspace navigation, private read cache and investor workflow improvements."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
