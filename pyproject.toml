[project]
name = "investment-engine"
version = "1.16.0"
description = "Formação do Investidor V1.16.0 with a self-hosted web interface, resilient market data and Google OIDC."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
