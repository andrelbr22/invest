[project]
name = "investment-engine"
version = "0.13.3"
description = "Formação do Investidor V1.12.3 with compact, visual and progressive-disclosure navigation."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
