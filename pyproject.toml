[project]
name = "investment-engine"
version = "0.8.3"
description = "Formação do Investidor V1.7.3 with fragment-based instant quantity controls and isolated portfolios."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
