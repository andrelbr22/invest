[project]
name = "investment-engine"
version = "0.8.0"
description = "Formação do Investidor V1.7.0 with isolated portfolios, weighted purchases and personal saved screeners."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
