[project]
name = "investment-engine"
version = "0.8.2"
description = "Formação do Investidor V1.7.2 with fixed quick quantity buttons and isolated multi-user portfolios."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
