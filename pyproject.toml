[project]
name = "investment-engine"
version = "0.7.1"
description = "Formação do Investidor V1.6.1 with cloud data synchronization, screening, portfolios and backtesting."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
