[project]
name = "investment-engine"
version = "0.8.5"
description = "Formação do Investidor V1.7.5 with startup-safe purchase calculation and fast quantity controls."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
