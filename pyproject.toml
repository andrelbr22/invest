[project]
name = "investment-engine"
version = "0.15.0"
description = "Formação do Investidor V1.14.0 with private multi-user price alerts and background e-mail delivery."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
