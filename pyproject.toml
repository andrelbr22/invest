[project]
name = "investment-engine"
version = "0.7.2"
description = "Formação do Investidor V1.6.2 with Google account permissions, read-only visitors, screening, portfolios and backtesting."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
