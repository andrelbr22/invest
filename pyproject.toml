[project]
name = "investment-engine"
version = "0.9.0"
description = "Formação do Investidor V1.8.0 with user-scoped asset universes and improved navigation."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
