# Patch V1.20.2

Não há nova migração de banco de dados. O patch altera provedores de mercado, cache diário, interface web e testes.

O cache principal mudou para `main-v6`, garantindo que o ambiente gere um novo retrato em vez de reutilizar o formato antigo.
